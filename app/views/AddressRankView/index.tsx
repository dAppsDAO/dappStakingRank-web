import { Link } from "@remix-run/react";
import { FC } from "react";

export const AddressRankView: FC = ({}) => {
  // const { publicKey } = useWallet();

  const onClick = () => {};

  return <div className="container mx-auto max-w-6xl p-8 2xl:px-0"></div>;
};
